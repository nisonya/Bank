<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bank">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bank"
        android:usesCleartextTraffic="true">
        <activity android:name=".New_card"></activity>
        <activity android:name=".Extract" />
        <activity android:name=".history.History" />
        <activity android:name=".Transaction" />
        <activity android:name=".registration" />
        <activity android:name=".Auth" />
        <activity android:name=".Assets" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCVBcLtG29Xm1A1p0asfDO4szjhS3KevQM" />

        <activity android:name=".Maps" />
        <activity android:name=".Profile" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>